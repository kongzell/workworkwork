{
  "name": "follow-up",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev:api\" \"npm run dev -w frontend\"",
    "dev:api": "node scripts/dev-api.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
